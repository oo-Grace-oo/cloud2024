<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.atguigu.cloud.mapper.PayMapper">
    <resultMap id="BaseResultMap" type="com.atguigu.cloud.entities.Pay">
        <id column="id" jdbcType="INTEGER" property="id"/>
        <result column="pay_no" jdbcType="VARCHAR" property="payNo"/>
        <result column="order_no" jdbcType="VARCHAR" property="orderNo"/>
        <result column="user_id" jdbcType="INTEGER" property="userId"/>
        <result column="amount" jdbcType="DECIMAL" property="amount"/>
        <result column="deleted" jdbcType="TINYINT" property="deleted"/>
        <result column="create_time" jdbcType="TIMESTAMP" property="createTime"/>
        <result column="update_time" jdbcType="TIMESTAMP" property="updateTime"/>
    </resultMap>
    <select id="selectColumns" resultType="java.util.Map">
        show columns from t_pay;
    </select>

    <select id="selectData" resultType="java.util.Map">
        select
        <foreach collection="col" item="item" separator=",">
            ${item}
        </foreach>
        from t_pay
        limit 0 ,200
    </select>

    <insert id="replaceInto">
        replace into t_pay
        <foreach collection="col" item="item" separator="," open="(" close=")">
            ${item}
        </foreach>
        values
        <foreach collection="datas" item="data" separator=",">
            (
            <foreach collection="col" item="item" separator=",">
                ${item}
            </foreach>
            )
        </foreach>
    </insert>

    <insert id="replaceIntoData">
        replace into t_pay
        <foreach collection="col" item="item" separator="," open="(" close=")">
            ${item}
        </foreach>
        values
        <foreach collection="col" item="colu" separator="," open="(" close=")">
            ${colu}
        </foreach>
    </insert>

</mapper>